# T-SQL Built-in Functions (Categorized)

## Aggregate

``` text
AVG CHECKSUM_AGG COUNT COUNT_BIG GROUPING GROUPING_ID MAX MIN STDEV STDEVP STRING_AGG SUM VAR VARP
```

## String

``` text
ASCII CHAR CHARINDEX CONCAT CONCAT_WS DIFFERENCE FORMAT LEFT LEN LOWER LTRIM NCHAR
PATINDEX QUOTENAME REPLACE REPLICATE REVERSE RIGHT RTRIM SOUNDEX SPACE STRING_ESCAPE
STRING_SPLIT STUFF SUBSTRING TRANSLATE TRIM UNICODE UPPER
```

## Date & Time

``` text
CURRENT_TIMESTAMP DATEADD DATEDIFF DATEDIFF_BIG DATEFROMPARTS DATENAME DATEPART
DATETIME2FROMPARTS DATETIMEFROMPARTS DAY EOMONTH GETDATE GETUTCDATE MONTH
SMALLDATETIMEFROMPARTS SYSDATETIME SYSDATETIMEOFFSET SYSUTCDATETIME TIMEFROMPARTS YEAR
```

## Mathematical

``` text
ABS ACOS ASIN ATAN ATN2 CEILING COS COT DEGREES EXP FLOOR LOG LOG10 PI POWER
RADIANS RAND ROUND SIGN SIN SQRT SQUARE TAN
```

## Conversion

``` text
CAST CONVERT PARSE TRY_CAST TRY_CONVERT TRY_PARSE
```

## NULL / Logical

``` text
CHOOSE COALESCE IIF ISNULL NULLIF
```

## JSON

``` text
ISJSON JSON_ARRAY JSON_MODIFY JSON_OBJECT JSON_PATH_EXISTS JSON_QUERY JSON_VALUE OPENJSON
```

## Metadata & Security

``` text
APP_NAME DB_ID DB_NAME HOST_NAME NEWID NEWSEQUENTIALID OBJECT_ID OBJECT_NAME
SCOPE_IDENTITY SESSION_CONTEXT SUSER_ID SUSER_NAME SUSER_SNAME USER_ID USER_NAME
```

## Cryptographic

``` text
CHECKSUM BINARY_CHECKSUM HASHBYTES
```

## Window / Analytic

``` text
CUME_DIST DENSE_RANK FIRST_VALUE LAG LAST_VALUE LEAD NTILE PERCENT_RANK
PERCENTILE_CONT PERCENTILE_DISC RANK ROW_NUMBER
```

## XML

``` text
FOR XML OPENXML
```

## Spatial

``` text
geometry::STGeomFromText geography::STGeomFromText
```
