Activity Monitor: Session tab
it is using the stored procedure sp_whoisactive
the source code is in the script https://github.com/amachanic/sp_whoisactive/blob/master/sp_WhoIsActive.sql
do not refresh autoatic. it is executed by the first tab load and then by pressing the refresh button.
you can use the result panel from the query editor
at the top add aknowledgemts and thanks:
https://github.com/amachanic/sp_whoisactive and Adam Machanic
add in the help -> about reference to this



sp_block


new tab: waits
at the top a section that show the grpahic from history: sql server aẃaits and from the sample the graphic the same one each occupy 50% the width


restore backup -> show remote server directoy, move files, error messages, text trimming etc

white/light style ? -> options, default dark

---------

reports
    - serverw
    - database
        - top tables
        - disk usage

-------------

- Availability Groups stuff

- query editor word wrap (display only, line numbers)
- external editor nvim, nano , etc


--------------

sp_blitz & index with reference built in

------

review both projects and check for bugs, inconsistencies, optimizations, simplifications , architecture and refractoring. think hard, take your time, be honest. plan only, ask before implementing to switch models. do not commit to git

show me the top 5 know issues if thre are any



bunx ccusage          # Show all detected sources by day (default)
bunx ccusage daily    # All detected sources by day
bunx ccusage weekly   # All detected sources by week
bunx ccusage monthly  # All detected sources by month
bunx ccusage session  # All detected sources by session
bunx ccusage blocks   # Claude Code 5-hour billing windows
bunx ccusage statusline  # Claude Code status line for hooks (Beta)


--- prepare for release
commit before both projects

prepare gosmo for release v0.0.7 . Update readme.md, release.md, changelog.md, plan.md and all other documentations and diagramms. the release document should contain only the changes from the last release. they should be summarized as bullet points and splitted in three cantegories. New, Fixes and Changes.
do not commit or tag anything to git. 


prepare gossms for release v0.0.5 . Update readme.md, release.md, changelog.md, plan.md and all other documentations. the release document should contain only the changes from the last release. they should be summarized as bullet points and splitted in three cantegories. New, Fixes and Changes.
do not commit or tag anything to git. 



