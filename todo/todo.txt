
**Phase 3 — the tree's missing families (M, ~3–4 weeks)**
13. Types, Assemblies, Rules, Defaults, Plan Guides, External Resources,
    Database Snapshots, Tables sub-folders
14. Service Broker (whole subtree)
15. Certificates / Symmetric / Asymmetric Keys, Database Audit Specs

**Phase 5 — the big ones (L, open-ended)**
23. Extended Events + XEvent Profiler viewer
24. Database Mail, Resource Governor
25. Live Query Statistics; Compare Showplan
26. Replication (read-only)
27. Full-Text; Maintenance-as-Agent-jobs; Data Collection (read-only)

-------------

new connect dialog design. keep both, configure in options which design to use.

- query editor :
    - word wrap (display only, line numbers)
    - auto indent ?
    - external editor nvim, nano , etc

new tab: waits
at the top a section that show the grpahic from history: sql server aẃaits and from the sample the graphic the same one each occupy 50% the width

shrink database

group by schema

white/light style ? -> options, default dark

keyboard shortcuts for mac (platform dependednt?

sp_blitz & index with reference built in

- application icons for win, macos, linux.
    - create the icons
    - change the build pipeline

--- bugs and optimizations ---------------------------------------------

review both projects and check for bugs, inconsistencies, optimizations, simplifications , architecture and refrctoring. think hard, take your time, be honest. plan only, ask before implementing to switch models. do not commit to git. create a plan file in docs/ to fix them

show me the top 15 know bugs,issues etc if thre are any. show only issues that can be fixed. create a plan file in docs/ to fix them

optimize and compact calude.md , architecture.md, plan.md etc in both projects

go over the source files code comments and compact them. throw unnecessary wording. do not keep history in comments only the actual state, unless necessary.


--- prepare for release -------------------------------------------------

commit before both projects

prepare gosmo for release v0.0.11 . Update readme.md, release.md, changelog.md, plan.md and all other documentations and diagramms. the release document should contain only the changes from the last release, delete the old release. they should be summarized as bullet points and splitted in three cantegories. New, Fixes and Changes. The texts should be short an concise, the long explanation is in the changelog.md
do not commit or tag anything to git. 

fix go.mod before, reference the new :

prepare gossms for release v0.0.9 . Update readme.md, release.md, changelog.md, plan.md and all other documentations and diagramms. the release document should contain only the changes from the last release, delete the old release information. they should be summarized as bullet points and splitted in three cantegories. New, Fixes and Changes. The texts should be short an concise, the long explanation is in the changelog.md
do not commit or tag anything to git. 


make the text in the RELEASE.md shorter and conciser. the long explanation is in CHANGELOG.md



--------------


read following articles:

https://www.anthropic.com/news/claude-opus-5 
https://claude.com/blog/the-new-rules-of-context-engineering-for-claude-5-generation-models  
 
based on this, fix my claude.md files on both projects and also the memory file
show me the changes before you write any change.